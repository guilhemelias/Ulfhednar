package iut.cours.lifehealth.model.sensor;

/**
 *  An interface to define a class that avert that the accelerometer sensor has changed
 */
public interface OnPedometerChangedListener {
    /**
     * a method to avert that the accelerometer sensor has changed
     */
    void onPedometerChanged();
}
